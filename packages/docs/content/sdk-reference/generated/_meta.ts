// Populated by `pnpm --filter @iln/sdk docs:generate` (typedoc), which writes
// generated `.mdx` pages plus their `_meta.ts` entries into this directory.
// This placeholder keeps the build unblocked when that step hasn't run.
export default {};
