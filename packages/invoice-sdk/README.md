# `@iln/invoice-sdk`

This package exists purely as a legacy re-export alias pointing to `@iln/sdk` to support an older import name.

**Note:** A workspace audit confirmed that there are no remaining consumers of this alias package. It is flagged for removal and will be deleted in a future update.
